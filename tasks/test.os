#Использовать 1testrunner
#Использовать "../src"

Процедура ВыполнитьТестирование()
	
	Тестер = Новый Тестер;

	КаталогПроекта = ОбъединитьПути(ТекущийСценарий().Каталог, "..");

	КаталогТестов = Новый Файл(ОбъединитьПути(КаталогПроекта, "tests"));
	
	РезультатТестирования = Тестер.ТестироватьКаталог(КаталогТестов);

КонецПроцедуры

ВыполнитьТестирование();